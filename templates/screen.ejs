import * as React from "react"
import { Text } from "../../shared/text"
import { NavigationScreenProps } from "react-navigation"
import { Screen } from "../../shared/screen"

export interface <%= props.pascalName %> extends NavigationScreenProps<{}> {
}

// @inject("mobxstuff")
@observer
export class <%= props.pascalName %> extends React.Component<RegisterScreenProps, {}> {
  render () {
    return (
      <Screen preset="fixedCenter">
        <Text preset="header" tx="<%= props.pascalName %>.header" />
      </Screen>
    )
  }
}
